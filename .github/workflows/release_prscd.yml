name: Build and release prscd

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build and release
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_UNSECURE_COMMANDS: true
    defaults:
      run:
        working-directory: prscd

    steps:
      - name: Check out
        uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.19
          cache-dependency-path: prscd/go.sum

      - name: Run build script
        env:
          PRSCD_PLATFORMS: 'darwin/amd64,darwin/arm64,windows/amd64,windows/arm64,linux/amd64,linux/arm64,freebsd/amd64,freebsd/arm64'
        run: |
          bash ./build.sh

      - name: Generate hashes
        run: |
          cd ./build
          for f in $(find . -type f); do
            sha256sum $f | sudo tee -a hashes.txt
          done

      - name: Upload
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ./prscd/build/prscd-darwin-amd64
            ./prscd/build/prscd-darwin-arm64
            ./prscd/build/prscd-windows-amd64.exe
            ./prscd/build/prscd-windows-arm64.exe
            ./prscd/build/prscd-linux-amd64
            ./prscd/build/prscd-linux-arm64
            ./prscd/build/prscd-freebsd-amd64
            ./prscd/build/prscd-freebsd-arm64
            ./prscd/build/hashes.txt
